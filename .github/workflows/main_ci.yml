name: CI - Quality Gate

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  quality-gate:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Planning phase - no code to test yet
      run: |
        echo "========================================="
        echo "OPTEK - Planning Phase Complete"
        echo "========================================="
        echo "Stack: Next.js 14 + Supabase + Claude API + Stripe"
        echo "Status: Documentation only (PLAN.md, ADRs, directives)"
        echo "Next: Sprint 1 will replace this with pnpm lint + type-check + test + build"
        echo "========================================="
